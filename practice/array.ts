/*let color: string[] = ['blue', 'green', 'red'];
color.splice(2, 0, 'purple', 'Magenta');
console.log(color);

color.forEach((elem, index) => {
  if (elem === 'Magenta') {
    color.splice(index, 1, 'magenta')
  }
});
console.log(color.length);

let newColor: string[] = color.slice(0, 2);
console.log(newColor);
console.log(); */

let numbers: number[] = [7, 12, 4, 1, 42, 101, 9, 72];
//console.log(numbers.sort((a, b) => { return b - a }));

let testObject = {
  height: 2,
  with: 10,
  name: 'Eva'
}
console.log(Object['values'](testObject));
console.log(testObject);











